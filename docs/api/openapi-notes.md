# MaternaLink OpenAPI Notes

Swagger is served at `http://localhost:3000/api/docs`.

Documented tags:

- `master`
- `inputs`
- `forecast`
- `lplpo`
- `distribution`

DTOs use normalized identifiers such as `puskesmasId`, `obatId`, `kondisiId`, and `gejalaId`. Forecast output is deterministic placeholder logic for API and database validation before ML integration.
