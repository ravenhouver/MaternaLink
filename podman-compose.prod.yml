services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-maternalink}
      POSTGRES_USER: ${POSTGRES_USER:-maternalink}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    volumes:
      - maternalink-prod-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-maternalink} -d ${POSTGRES_DB:-maternalink}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  api:
    image: ${API_IMAGE:-ghcr.io/ravenhouver/maternalink-api:latest}
    environment:
      NODE_ENV: production
      PORT: 3001
      DB_HOST: postgres
      DB_PORT: 5432
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL is required}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      WEB_ORIGIN: ${WEB_ORIGIN:-http://172.93.186.62:41873}
      AI_SERVICE_BASE_URL: ${AI_SERVICE_BASE_URL:-https://azrilfahmiardi-maternalink-ai.hf.space}
      AI_MODE: ${AI_MODE:-remote}
      AI_SERVICE_TIMEOUT_MS: ${AI_SERVICE_TIMEOUT_MS:-30000}
      AI_LAYER2_TIMEOUT_MS: ${AI_LAYER2_TIMEOUT_MS:-600000}
      SPEECH_STT_SERVICE_URL: ${SPEECH_STT_SERVICE_URL:-http://speech-stt:8002}
      SPEECH_STT_TIMEOUT_MS: ${SPEECH_STT_TIMEOUT_MS:-120000}
      RUN_SEED: ${RUN_SEED:-true}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-false}
    ports:
      - "41874:3001"
    depends_on:
      postgres:
        condition: service_healthy
      speech-stt:
        condition: service_started
    restart: unless-stopped

  speech-stt:
    image: ${SPEECH_STT_IMAGE:-ghcr.io/ravenhouver/maternalink-speech-stt:latest}
    environment:
      STT_MODEL_SIZE: ${STT_MODEL_SIZE:-small}
      STT_DEVICE: ${STT_DEVICE:-cpu}
      STT_COMPUTE_TYPE: ${STT_COMPUTE_TYPE:-int8}
    ports:
      - "41875:8002"
    restart: unless-stopped

  web:
    image: ${WEB_IMAGE:-ghcr.io/ravenhouver/maternalink-web:latest}
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://172.93.186.62:41874/api}
    ports:
      - "41873:3000"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  maternalink-prod-postgres-data:
