import { LoginPageContent } from '@/features/login/login-page-content';

export const metadata = {
  title: 'Login | MaternaLink',
  description: 'Masuk ke sistem MaternaLink.',
};

export default function LoginPage() {
  return <LoginPageContent />;
}
